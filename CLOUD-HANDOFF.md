# MaxRecord クラウド作業引継ぎ

目的: ユーザー提供の改修構想を既存のMaxRecordに適用し、既存データを保持して検証後に公開できる状態にする。
このブランチは未公開の改修案。レビュー・修正・検証を続けてください。

## 必要な機能
- スマホ向けの記録・履歴、週間分析・カルテ。
- ドライフードを配膳gと廃棄gで記録し、差分を実食量とする。日付をまたいでも廃棄日で集計。複数配膳に対応。
- 食事（ウェット・おやつ等）、水、トイレ、補液・食欲増進剤、異変、気付きメモ。
- 新規トイレでは尿と便を選択し、「両方」は新規選択に出さない。旧記録の「うんこ」「うんち」「両方」は消失・誤変換させない。
- 既存ドライ記録の回数・量の表現を維持し、グラム不明を0gとして扱わない。
- 通院用のA4横レポート。グラフと数値表の日付軸を揃え、尿・便を区別し、実際の補液・増進剤実施とデータ取得日時を表示。
- 今回ユーザーが貼ったコードでは補液は有無のみ。量の医学的な推奨・変更をしない。

## 実装の出自と制限
ユーザーは新しいHTML全文をチャットに提示した。現在の改修案はその全文の直接置換ではなく、現行mainへ機能を統合したもの。見た目や細かい入力選択肢は完全一致していない。
従来のJSON保存・日付指定分析・水分サプリを残している。
テストはNodeで4件成功。計算と模擬Firestoreトランザクションのテストであり、実ブラウザ・実Firebaseでの動作保証ではない。

## 次にやる作業
1. 全ファイルと差分をレビューし、不具合を修正する。
2. Vueを実際に読み込むブラウザで全カテゴリの登録・編集・削除、複数配膳、二重廃棄、廃棄削除後の再開、旧データの表示・編集を確認。テスト用データはモックか隔離環境のみで、本番DBに書き込まない。
3. 接続失敗・空データ・通信復旧・日付変更を確認。日付のリアクティブ更新と、すべての分析で旧排便表記を一貫して扱うことを特に確認。
4. 食事種類変更時に旧フォーム値が混ざらないこと、未確定配膳を食事回数に二重算入しないことを確認。
5. 390px程度のスマホ表示とA4横PDFを実際にレンダリング。印刷のグラフ・数値の位置と読みやすさ、1ページに収まることを確認。
6. 既存Firestoreコレクションのパスを変更しない。ルールを公開化しない。データを移行・削除しない。
7. 変更案のPRを更新し、結果と未検証事項を記録。公開前に現行データのJSONバックアップを確保し、検証済み差分のみ本番へ反映。権限不足や必要な本人操作は具体的に説明する。

## 2026-09-12 レビュー追記
- 廃棄ドライの従来の日付指定分析・個別グラフも `effectiveDate`（廃棄日）を参照するよう修正。
- 旧「うんこ」「うんち」「両方」と「尿」「便」を共通判定に集約し、週次・日付指定・個別グラフで尿便の意味を統一。新規入力は「おしっこ」「うんち」のみ、旧値の編集時は元の選択肢を残す。
- Nodeテスト5件と差分検査は成功。本番DBへの書き込み、本番公開、mainへのマージは実施していない。
- 390px表示とA4横PDFの構造条件は自動検査したが、環境内にブラウザがなく、Chromium/Playwrightの導入も外部通信403で失敗したため、実レンダリング確認とスクリーンショット取得は未完了。ブラウザを利用できる隔離環境で必ず最終確認すること。

## 基準と復元
基準main: c3e99d539053a96fe62c73fe20f0f76691e9d2ce
公開URL: https://hd4svw9v.github.io/MaxRecord/
基準index.htmlへの復元はGit履歴から可能。ただしコード復元とDBデータ復元は別。
