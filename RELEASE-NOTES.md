# MaxRecord 改修案（未公開・ブラウザ実画面検証待ち）

提供された改修案の機能を、取得した現行コードに統合しています。提供HTMLそのものの完全な置換ではありません。従来のJSON保存、日付指定、回数分析、水分サプリを残しています。

- ドライフードの配膳・廃棄・まとめて入力、廃棄日で実食集計
- 複数配膳、補液・食欲増進剤、気付きメモ、任意の食事グラム・飲水ml
- Firestoreトランザクションで配膳と廃棄を一括更新。廃棄の二重登録を防ぎ、廃棄削除で配膳を再開
- 旧ドライ記録は量不明の回数記録として保持。旧「うんこ」「うんち」「両方」と新しい「尿」「便」をすべての分析で一貫して算入
- デモデータなし。接続エラー時に保存成功を偽装しない
- 週次統合グラフ、日別表、印刷用A4横レポート。印刷にはブラウザのPDF保存を使用
- 印刷メモは最大5件、各100文字。超過はアプリ参照と明記
- 補液は今回提示コードに合わせて有無のみ（量の入力欄なし）

## 検証
`node --test tests/health.test.mjs` : 5テスト成功。
入力数値、日跨ぎ、旧データ、模擬トランザクションを用いた登録・編集・削除・重複防止、オフライン時の保存抑止。
JavaScript構文検査とgit diff --check成功。
レビューで、従来の日付指定分析と個別グラフが廃棄日ではなく記録日時を参照していた問題、および旧排便表記の集計漏れを修正しました。390px向けviewport・最大幅・横スクロール、A4横の用紙指定・印刷専用領域・SVG寸法は自動検査済みです。

ただし、この実行環境にはブラウザがなく、Chromium/Playwrightの取得もネットワークポリシー（HTTP 403）で失敗しました。そのため、実ブラウザでの390pxスクリーンショットとPDF生成・1ページ確認は未完了です。「実際に確認済み」とはしていません。実Firestore、複数端末、通信復旧も未検証であり、公開前の隔離ブラウザ確認が必要です。本番DBへの書き込み、本番公開、mainへのマージは行っていません。

## 公開状況
初回のGitHub書き込みは403で失敗。その後ユーザーがGitHub Appをインストールし、クラウド検証用ブランチへの保存を再開。本番コード・DBは未変更。
基準コミット: c3e99d539053a96fe62c73fe20f0f76691e9d2ce
現行index.html blob: 48551a35fa11bcdb88327ec4c5d6cf01df79e6a6

公開前に実画面・印刷確認、実データのJSONバックアップ、Firestoreルールがトランザクションを許可することの確認が必要です。
公開時はindex.htmlとhealth-core.mjsを同じディレクトリへ一括反映してください。
rollback/index.htmlは取得時点の元コードです。コードの復元はDBの復元ではありません。
